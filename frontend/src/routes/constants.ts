export enum ROUTES {
  HOME = "/",
  LOGIN = "/login",
  NEWS = "/news",
  USERS = "/users",
  ERROR = "/*",
  BOOKS = "/books",
  BOOK = "/books/:id",
  USER_FOUND_BY_STRING = "/users/:searchUsers",
  USER_FOUND_BY_ID = "/users/:idUser",
}
